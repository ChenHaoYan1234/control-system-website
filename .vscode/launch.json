{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "运行脚本: dev",
            "request": "launch",
            "command": "yarn run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}